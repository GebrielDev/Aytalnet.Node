{
  "name": "aytalnet-fleet-management",
  "version": "1.0.0",
  "main": "backend/dist/index.js",
  "private": true,
  "workspaces": [
    "backend",
    "web-dashboard"
  ],
  "scripts": {
    "build": "npm run build --workspace=web-dashboard && npm run build --workspace=backend",
    "start": "node backend/dist/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
